const expect = require('expect');
const trails = require('./trails');

it('should clear the trails.json file', () => {
  expect(true).toBe(true).toBeA('number');
  // throw new Error('need to actually call function here');
});